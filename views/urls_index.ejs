<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <% include ./partials/_header %>
 </head>
 <body>
  <h2> List of URLS </h2>




      <% for(var url in urls) { %>
<% if ((urls[url].uid) === (user_id)) { %>

<ul>
        <li><%= urls[url].shortURL %> - <%= urls[url].longURL %> </li>
        <form method="POST" action="/urls/<%= url%>/delete">
        <button type="submit"> Delete </button>


        </form>

  </ul>
        <% } %>
           <% } %>

  </ul>


  <a href=" ">Click here to shorten URL</a>

 </body>
 </html>

